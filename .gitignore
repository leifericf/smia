.local
.cpcache/
.lsp/
.clj-kondo/
.nrepl-port
target/
# Anchored to the repo root: this is the build output dir. An unanchored
# `build/` would also swallow source packages like src/smia/build/.
/build/
*.log
.DS_Store
# Local agent/MCP configuration — not part of the project.
CLAUDE.md
.mcp.json
.claude/
# CLJS compiler state (dev-only; island bundles compile on demand
# into a temp-dir cache, never into the repo).
.shadow-cljs/
