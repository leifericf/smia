.local
.cpcache/
.lsp/
.clj-kondo/
.nrepl-port
target/
# Anchored to the repo root: this is the build output dir. An unanchored
# `build/` would also swallow source packages like src/clj_book/build/.
/build/
*.log
.DS_Store
# Local agent/MCP configuration — not part of the project.
CLAUDE.md
.mcp.json
.claude/
